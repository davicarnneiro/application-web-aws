name: CI/CD Pipeline

on:
  push:
    branches:
      - main # Configure para a sua branch principal

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Código
      uses: actions/checkout@v2

    - name: Build e Push da Imagem Docker
      env:
        DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
      run: |
        docker build -t web-app:latest .
        echo $DOCKERHUB_PASSWORD | docker login -u $DOCKERHUB_USERNAME --password-stdin
        docker tag web-app:latest web-app:1.0
        docker push web-app:latest
        docker push web-app:1.0

    # - name: Configurar Kubeconfig
    #   run: |
    #     echo ${{ secrets.KUBE_CONFIG_DATA }} | base64 -d > kubeconfig.yaml

    # - name: Deploy no Kubernetes
    #   run: |
    #     kubectl --kubeconfig=kubeconfig.yaml apply -f seus-arquivos-de-deploy.yaml






# name: CI/CD para Docker Hub e Kubernetes

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Verificar código-fonte
#       uses: actions/checkout@v4

#     - name: Log in to Docker Hub
#       uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a
#       with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}


#     - name: Build Docker images
#       run: docker build -t web-app:latest .

#     - name: Push Docker images
#       run: docker push web-app:latest

    # - name: Construir a imagem Docker
    #   run: docker build -t web-app:latest .

    # - name: Fazer push da imagem para o Docker Hub
    #   run: docker push web-app:latest

  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Configurar o Kubernetes
  #     uses: azure/k8s-set-context@v1
  #     with:
  #       kubeconfig: ${{ secrets.KUBE_CONFIG }}
  #       namespace: defaul

  #   - name: Aplicar as configurações no Kubernetes
  #     run: kubectl apply -f k8s/deployment.yaml
