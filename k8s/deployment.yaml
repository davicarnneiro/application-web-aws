apiVersion: apps/v1
kind: Deployment
metadata:
  name: phibro-teste-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: phibro-teste-app
  template:
    metadata:
      labels:
        app: phibro-teste-app
    spec:
      containers:
        - name: web-app-repo
          image: 144056461902.dkr.ecr.us-west-1.amazonaws.com/web-app-repo:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: phibro-teste-app
spec:
  selector:
    app: phibro-teste-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer

